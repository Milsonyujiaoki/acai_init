interface SimpleDialogProps {
    open: boolean;
    onOpenChange: (open: boolean) => void;
    title: string;
    children: React.ReactNode;
}
export declare function SimpleDialog({ open, onOpenChange, title, children }: SimpleDialogProps): any;
export {};
//# sourceMappingURL=SimpleDialog.d.ts.map